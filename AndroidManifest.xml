<?xml version="1.0" encoding="utf-8"?>
<manifest package="ru.istina"
    android:versionCode="4"
    android:installLocation="preferExternal"
    android:versionName="1.3" xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
   <uses-permission android:name="com.android.vending.BILLING" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/istina"
        android:label="@string/app_name" 
        android:theme="@style/AppBaseTheme"
         >
        
        <activity
            android:name="ru.istina.MainActivity"
             android:icon="@drawable/istina"
             android:configChanges="keyboardHidden|orientation|screenSize"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="PostDetailActivity"
            android:configChanges="keyboardHidden|orientation|screenSize">
        </activity>
        <intent-filter></intent-filter>
        <activity android:name="GalleryImageView"
            android:configChanges="keyboardHidden|orientation|screenSize">
            <intent-filter></intent-filter>
        </activity>
        <activity android:name="LoadWebView"
            android:configChanges="keyboardHidden|orientation|screenSize"></activity>
        <intent-filter></intent-filter>
    </application>

</manifest>
